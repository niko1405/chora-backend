info:
  name: Nicht-vorhandener Nachname
  type: http
  seq: 4

http:
  method: GET
  url: "{{restUrl}}?Nachname=Xxx"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: Nachname
      value: Xxx
      type: query
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
