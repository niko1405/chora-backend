info:
  name: Readiness
  type: http
  seq: 2

http:
  method: GET
  url: "{{healthUrl}}/readiness"
  headers:
    - name: ""
      value: ""
      disabled: true
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.headers['content-type']
      operator: eq
      value: application/json
    - expression: res.body.db
      operator: eq
      value: up

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
