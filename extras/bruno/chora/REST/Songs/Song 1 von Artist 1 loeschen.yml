info:
  name: Song 1 von Artist 1 loeschen
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{restArtistsUrl}}/:artistId/songs/:songId"
  params:
    - name: artistId
      value: "1"
      type: path
    - name: songId
      value: "999999"
      type: path

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
