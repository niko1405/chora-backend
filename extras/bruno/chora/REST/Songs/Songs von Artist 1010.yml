info:
  name: Songs von Artist 1010
  type: http
  seq: 2

http:
  method: GET
  url: "{{restSongsUrl}}?artist_id=1010"
  params:
    - name: artist_id
      value: "1010"
      type: query

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.body.content
      operator: isArray

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
