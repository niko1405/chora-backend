info:
  name: Songs von Artist 1010
  type: http
  seq: 1

http:
  method: GET
  url: "{{restArtistsUrl}}/:artistId/songs"
  params:
    - name: artistId
      value: "1010"
      type: path

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.body.content
      operator: isArray

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
