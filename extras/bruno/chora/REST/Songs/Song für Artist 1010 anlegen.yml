info:
  name: Song für Artist 1010 anlegen
  type: http
  seq: 4

http:
  method: POST
  url: "{{restArtistsUrl}}/:artistId/songs"
  params:
    - name: artistId
      value: "1010"
      type: path
  body:
    type: json
    data: |-
      {
        "titel": "Bruno Song Neu",
        "genres": ["Jazz"],
        "erscheinungsdatum": "2026-05-01",
        "dauer": 190
      }

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "201"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
