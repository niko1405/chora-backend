info:
  name: Song von Artist
  type: http
  seq: 2

http:
  method: GET
  url: "{{restArtistsUrl}}/:artistId/songs/:songId"
  params:
    - name: artistId
      value: "1010"
      type: path
    - name: songId
      value: "3010"
      type: path

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
