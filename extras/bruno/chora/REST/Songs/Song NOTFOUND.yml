info:
  name: Song NOTFOUND
  type: http
  seq: 4

http:
  method: GET
  url: "{{restSongsUrl}}/:songId"
  params:
    - name: songId
      value: "99999"
      type: path

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
