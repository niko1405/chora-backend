info:
  name: Song 3010 loeschen
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{restSongsUrl}}/:songId"
  params:
    - name: songId
      value: "3010"
      type: path
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "204"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
