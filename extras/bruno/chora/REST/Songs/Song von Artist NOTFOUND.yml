info:
  name: Song von Artist NOTFOUND
  type: http
  seq: 3

http:
  method: GET
  url: "{{restArtistsUrl}}/:artistId/songs/:songId"
  params:
    - name: artistId
      value: "1000"
      type: path
    - name: songId
      value: "3010"
      type: path

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
