info:
  name: REST
  type: folder
  seq: 1

request:
  auth: inherit
  variables:
    - name: restArtistsUrl
      value: "{{baseUrl}}/rest/artists"
    - name: restSongsUrl
      value: "{{baseUrl}}/rest/songs"
