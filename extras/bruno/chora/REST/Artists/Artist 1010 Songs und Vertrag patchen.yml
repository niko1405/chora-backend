info:
  name: Artist 1010 Songs und Vertrag patchen
  type: http
  seq: 9

http:
  method: PATCH
  url: "{{restArtistsUrl}}/:artistId"
  headers:
    - name: If-Match
      value: '"2"'
  params:
    - name: artistId
      value: "1010"
      type: path
  body:
    type: json
    data: |-
      {
        "songs": []
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "204"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
