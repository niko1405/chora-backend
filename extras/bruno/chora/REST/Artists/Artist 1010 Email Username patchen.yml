info:
  name: "Artist 1010 Email Username patchen "
  type: http
  seq: 8

http:
  method: PATCH
  url: "{{restArtistsUrl}}/:artistId"
  headers:
    - name: If-Match
      value: '"2"'
  params:
    - name: artistId
      value: "1051"
      type: path
  body:
    type: json
    data: |-
      {
        "email": "bruno.echo@acme.de",
        "username": "test"
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
