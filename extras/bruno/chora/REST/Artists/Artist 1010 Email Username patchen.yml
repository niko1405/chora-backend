info:
  name: "Artist 1010 Email Username patchen "
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{restArtistsUrl}}/:artistId"
  headers:
    - name: If-Match
      value: '"2"'
  params:
    - name: artistId
      value: "1051"
      type: path
  body:
    type: json
    data: |-
      {
        "email": "bruno.patch@acme.de",
        "username": "testpatch"
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "204"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
