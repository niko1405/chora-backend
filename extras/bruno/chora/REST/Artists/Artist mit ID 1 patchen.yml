info:
  name: Artist mit ID 1 patchen
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{restArtistsUrl}}/:artistId"
  auth: inherit
  headers:
    - name: If-Match
      value: '"1"'
  params:
    - name: artistId
      value: "999999"
      type: path
  body:
    type: json
    data: |-
      {
        "email": "artist.patch@example.com"
      }

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
