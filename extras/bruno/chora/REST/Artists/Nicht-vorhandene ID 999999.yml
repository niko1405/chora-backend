info:
  name: Nicht-vorhandene ID 999999
  type: http
  seq: 4

http:
  method: GET
  url: "{{restArtistsUrl}}/:id"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: id
      value: "999999"
      type: path
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
