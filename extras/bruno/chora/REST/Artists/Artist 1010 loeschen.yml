info:
  name: Artist 1010 loeschen
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{restArtistsUrl}}/:artistId"
  params:
    - name: artistId
      value: "1010"
      type: path
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
